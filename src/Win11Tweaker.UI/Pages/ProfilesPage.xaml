<Page x:Class="Win11Tweaker.UI.Pages.ProfilesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      Title="Profiles">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,24" MaxWidth="680">

            <TextBlock Text="Profiles" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="Save, export, and import your complete customization setups as .w11theme files."
                       FontSize="13"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                       Margin="0,0,0,24"/>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,20">
                <Button Content="📸 Save Current State"
                        Click="SaveCurrentState_Click"
                        Style="{DynamicResource AccentButtonStyle}"/>
                <Button Content="📥 Import .w11theme"
                        Click="ImportProfile_Click"/>
            </StackPanel>

            <!-- Profile cards -->
            <TextBlock Text="SAVED PROFILES" FontSize="11" FontWeight="SemiBold"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                       Margin="0,0,0,8"/>

            <ItemsControl x:Name="ProfilesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Preview screenshot -->
                                <Border Grid.Column="0" CornerRadius="4,0,0,4"
                                        Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                                        Height="90" ClipToBounds="True">
                                    <Image Source="{Binding PreviewPath}"
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>

                                <!-- Info -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center"
                                            Margin="16,0">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}"
                                               FontSize="12" TextWrapping="Wrap"
                                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                               Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding CreatedAtFormatted}"
                                               FontSize="11"
                                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center"
                                            Margin="8" Spacing="6">
                                    <Button Content="Apply"
                                            Tag="{Binding Id}"
                                            Click="ApplyProfile_Click"
                                            Style="{DynamicResource AccentButtonStyle}"
                                            HorizontalAlignment="Stretch"/>
                                    <Button Content="Export"
                                            Tag="{Binding Id}"
                                            Click="ExportProfile_Click"
                                            HorizontalAlignment="Stretch"/>
                                    <Button Content="Delete"
                                            Tag="{Binding Id}"
                                            Click="DeleteProfile_Click"
                                            HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="NoProfilesText"
                       Text="No saved profiles yet. Click 'Save Current State' to create one."
                       FontSize="13" Margin="0,8"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>

        </StackPanel>
    </ScrollViewer>
</Page>
